---
phase: 21-fork-curation-visibility-strategy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/21-fork-curation-visibility-strategy/before-state.json
  - .planning/phases/21-fork-curation-visibility-strategy/after-state.json
autonomous: false
requirements:
  - FORK-01
  - FORK-02

must_haves:
  truths:
    - "5 eLearning forks (SCOBot, lms-content-template-scorm2004e3, dewordify, wai-tutorials, aria-practices) are archived (public + read-only)"
    - "get-shit-done fork is NOT archived (remains active)"
    - "All 8 public repos remain public (count unchanged from Phase 20)"
    - "Evaluator/researcher descriptions on archived forks are intact"
    - "Domain-specific topics on archived forks are intact"
    - "24 previously privatized hobby forks remain private"
  artifacts:
    - path: ".planning/phases/21-fork-curation-visibility-strategy/before-state.json"
      provides: "Pre-archive snapshot of all public repo states"
    - path: ".planning/phases/21-fork-curation-visibility-strategy/after-state.json"
      provides: "Post-archive snapshot confirming archive status and metadata integrity"
  key_links:
    - from: "gh repo archive command"
      to: "5 eLearning fork repos"
      via: "GitHub CLI archive operation with --confirm flag"
      pattern: "gh repo archive novakda\\.* --confirm"
    - from: "after-state.json"
      to: "before-state.json"
      via: "Comparison shows only isArchived changed (descriptions/topics preserved)"
      pattern: "isArchived.*true"
---

<objective>
Archive 5 eLearning forks and verify profile integrity

Purpose: Make eLearning forks read-only (archived) to signal "evaluated and preserved" rather than "actively maintained," reinforcing the evaluator/researcher positioning established in Phase 20. Keep get-shit-done active as a dev tool in use.

Output: 5 archived eLearning forks, 1 active dev tool fork, before/after state snapshots, 4-dimension verification (repo count, archive status, descriptions, topics)
</objective>

<execution_context>
@/home/xhiris/.claude/get-shit-done/workflows/execute-plan.md
@/home/xhiris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-fork-curation-visibility-strategy/21-CONTEXT.md
@.planning/phases/21-fork-curation-visibility-strategy/21-RESEARCH.md
@.planning/phases/20-repository-metadata-discoverability/20-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture before-state and archive 5 eLearning forks</name>
  <files>
    .planning/phases/21-fork-curation-visibility-strategy/before-state.json
  </files>
  <action>
    Step 1: Capture before-state snapshot of all public repos (descriptions, topics, archive status, fork status):
    ```bash
    gh repo list novakda --limit 50 --json name,description,isArchived,isFork,isPrivate,repositoryTopics | \
      jq '[.[] | select(.isPrivate == false)] | sort_by(.name)' \
      > .planning/phases/21-fork-curation-visibility-strategy/before-state.json
    ```

    Step 2: Archive the 5 eLearning forks sequentially using `gh repo archive` with `--confirm` flag (non-interactive):
    ```bash
    gh repo archive novakda/aria-practices --confirm
    gh repo archive novakda/dewordify --confirm
    gh repo archive novakda/lms-content-template-scorm2004e3 --confirm
    gh repo archive novakda/SCOBot --confirm
    gh repo archive novakda/wai-tutorials --confirm
    ```

    Step 3: Verify each archive succeeded by checking isArchived field:
    ```bash
    for repo in aria-practices dewordify lms-content-template-scorm2004e3 SCOBot wai-tutorials; do
      gh repo view novakda/$repo --json isArchived --jq '.isArchived'
    done
    ```
    All 5 must return `true`.

    Step 4: Verify get-shit-done is NOT archived:
    ```bash
    gh repo view novakda/get-shit-done --json isArchived --jq '.isArchived'
    ```
    Must return `false`.

    IMPORTANT: Do NOT archive get-shit-done — it is an active dev tool per user decision.
    IMPORTANT: Do NOT modify any descriptions or topics — Phase 20 metadata stays as-is.
  </action>
  <verify>
    Run: `gh repo list novakda --limit 50 --json name,isFork,isArchived,isPrivate | jq '[.[] | select(.isPrivate == false and .isFork == true)] | map({name, isArchived}) | sort_by(.name)'`

    Expected output:
    ```json
    [
      {"name": "aria-practices", "isArchived": true},
      {"name": "dewordify", "isArchived": true},
      {"name": "get-shit-done", "isArchived": false},
      {"name": "lms-content-template-scorm2004e3", "isArchived": true},
      {"name": "SCOBot", "isArchived": true},
      {"name": "wai-tutorials", "isArchived": true}
    ]
    ```
  </verify>
  <done>5 eLearning forks archived, get-shit-done remains active, before-state.json captured</done>
</task>

<task type="auto">
  <name>Task 2: Comprehensive 4-dimension verification and after-state capture</name>
  <files>
    .planning/phases/21-fork-curation-visibility-strategy/after-state.json
  </files>
  <action>
    Run the full 4-dimension verification suite per user constraints (CONTEXT.md specifies "automated check must verify all 4"):

    **Check 1 — Public repo count (must be exactly 8):**
    ```bash
    gh repo list novakda --limit 50 --json name,isPrivate | \
      jq '[.[] | select(.isPrivate == false)] | length'
    ```
    Expected: `8`

    **Check 2 — Archive status (5 archived forks, 1 active fork, 2 originals):**
    ```bash
    gh repo list novakda --limit 50 --json name,isFork,isArchived,isPrivate | \
      jq '[.[] | select(.isPrivate == false)] | map({name, isFork, isArchived}) | sort_by(.name)'
    ```
    Expected: aria-practices(true,true), dewordify(true,true), get-shit-done(true,false), lms-content-template-scorm2004e3(true,true), novakda(false,false), pattern158.solutions(false,false), SCOBot(true,true), wai-tutorials(true,true)

    **Check 3 — Description integrity (evaluator framing preserved on 5 forks):**
    ```bash
    for repo in aria-practices wai-tutorials dewordify lms-content-template-scorm2004e3 SCOBot; do
      echo "=== $repo ==="
      gh repo view novakda/$repo --json description --jq '.description'
    done
    ```
    All 5 must contain "Fork of" and "Evaluated" in their descriptions.

    **Check 4 — Topic integrity (domain topics preserved):**
    ```bash
    for repo in aria-practices wai-tutorials dewordify lms-content-template-scorm2004e3 SCOBot; do
      echo "=== $repo ==="
      gh api /repos/novakda/$repo/topics -H "Accept: application/vnd.github.mercy-preview+json" | \
        jq -r '.names | join(", ")'
    done
    ```
    Verify topic counts match Phase 20 state: aria-practices(4), wai-tutorials(4), dewordify(4), lms-content-template(4), SCOBot(5).

    **Capture after-state:**
    ```bash
    gh repo list novakda --limit 50 --json name,description,isArchived,isFork,isPrivate,repositoryTopics | \
      jq '[.[] | select(.isPrivate == false)] | sort_by(.name)' \
      > .planning/phases/21-fork-curation-visibility-strategy/after-state.json
    ```

    **Assess "Archived" badge appearance (Claude's discretion):**
    Note whether the archived badge on the 5 eLearning forks is concerning for the professional aesthetic. The badge should reinforce evaluator positioning ("evaluated and preserved") rather than undermine it. Flag to user during checkpoint if badge appearance seems problematic.

    If ANY of the 4 checks fail, stop and diagnose before proceeding.
  </action>
  <verify>
    Run: `cat .planning/phases/21-fork-curation-visibility-strategy/after-state.json | jq 'length'`
    Expected: `8` (8 public repos in after-state)

    Run: `cat .planning/phases/21-fork-curation-visibility-strategy/after-state.json | jq '[.[] | select(.isArchived == true)] | length'`
    Expected: `5` (5 archived repos)
  </verify>
  <done>All 4 verification dimensions pass, after-state.json captured as audit trail</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual profile review</name>
  <action>Human verification checkpoint — no automated action. User reviews GitHub profile to confirm archive operations and metadata integrity look correct visually.</action>
  <what-built>5 eLearning forks archived (public + read-only with "Archived" badge), get-shit-done active, profile metadata intact</what-built>
  <how-to-verify>
    1. Visit https://github.com/novakda
    2. Verify 8 public repos visible (2 originals + 6 forks)
    3. Check that 5 eLearning forks show "Archived" badge (aria-practices, dewordify, lms-content-template-scorm2004e3, SCOBot, wai-tutorials)
    4. Check that get-shit-done does NOT show "Archived" badge
    5. Verify "Archived" badge appearance is acceptable for professional presentation (does it reinforce or undermine evaluator positioning?)
    6. Confirm descriptions and topics are visible on archived forks
    7. Overall impression: does the profile look focused and professional rather than cluttered?
  </how-to-verify>
  <verify>User confirms profile appearance via checkpoint approval</verify>
  <done>User types "approved" confirming profile looks professional and archive operations are correct</done>
  <resume-signal>Type "approved" to complete phase, or describe any issues with the profile appearance</resume-signal>
</task>

</tasks>

<verification>
Phase 21 verification criteria (all must pass):
1. Public repo count = 8 (unchanged from Phase 20)
2. 5 eLearning forks: isArchived = true, isPrivate = false
3. get-shit-done: isArchived = false, isPrivate = false
4. 2 originals: isArchived = false, isPrivate = false
5. All evaluator/researcher descriptions preserved (contain "Fork of" and "Evaluated")
6. All domain topics preserved (counts: 4, 4, 4, 4, 5)
7. before-state.json and after-state.json exist as audit trail
8. User confirms profile looks professional via visual checkpoint
</verification>

<success_criteria>
- 5 eLearning forks archived, signaling "evaluated and preserved"
- get-shit-done remains active and unarchived
- Profile metadata (descriptions, topics) fully intact post-archive
- User confirms professional appearance via checkpoint
- Complete audit trail (before-state and after-state snapshots)
</success_criteria>

<output>
After completion, create `.planning/phases/21-fork-curation-visibility-strategy/21-01-SUMMARY.md`
</output>
