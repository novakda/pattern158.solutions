---
phase: 20-repository-metadata-discoverability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [META-01, META-02, META-03, META-04]

must_haves:
  truths:
    - "pattern158.solutions repo displays professional description with Pattern 158 brand name"
    - "pattern158.solutions repo shows homepage URL linking to https://pattern158.solutions"
    - "pattern158.solutions repo has domain-specific topics (portfolio, accessibility, wcag, static-site)"
    - "novakda profile README repo has appropriate description"
    - "31 non-professional repos are no longer visible on the public profile"
    - "Pre-change baseline snapshot exists for rollback reference"
  artifacts:
    - path: ".planning/phases/20-repository-metadata-discoverability/before-state.json"
      provides: "Baseline snapshot of all repos before changes"
    - path: "external:novakda/pattern158.solutions metadata"
      provides: "Updated description, homepage URL, and topics"
    - path: "external:novakda/novakda metadata"
      provides: "Updated description"
  key_links:
    - from: "gh repo edit novakda/pattern158.solutions"
      to: "GitHub repository settings"
      via: "gh CLI --description, --homepage, --add-topic flags"
      pattern: "gh repo edit.*--description.*--homepage"
    - from: "gh repo edit novakda/{repo} --visibility private"
      to: "GitHub visibility settings"
      via: "gh CLI visibility flag for 31 repos"
      pattern: "gh repo edit.*--visibility private"
---

<objective>
Clean up profile visibility and establish flagship repository metadata.

Purpose: Remove non-professional repos from public profile and set up pattern158.solutions + novakda repos with professional descriptions, homepage URL, and discoverability topics. This creates the foundation for a curated, professional GitHub presence.

Output: 31 repos made private, pattern158.solutions and novakda repos with complete metadata, baseline snapshot saved.
</objective>

<execution_context>
@/home/xhiris/.claude/get-shit-done/workflows/execute-plan.md
@/home/xhiris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-repository-metadata-discoverability/20-CONTEXT.md
@.planning/phases/20-repository-metadata-discoverability/20-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture baseline snapshot and make 31 repos private</name>
  <files>.planning/phases/20-repository-metadata-discoverability/before-state.json</files>
  <action>
Step 1 — Capture pre-change baseline of all repos:
```bash
gh repo list novakda --limit 200 --json name,isPrivate,isFork,description,homepageUrl,repositoryTopics > .planning/phases/20-repository-metadata-discoverability/before-state.json
```

Step 2 — Make 7 original repos private (batch 1):
```bash
for repo in clean-scraper-extension fo4_addonideas github-for-developers-7 imatch-deviantart-api imatch-imgops tabbles-tools tabbles4-plugin-wlpg; do
  gh repo edit novakda/$repo --visibility private
  echo "Made private: $repo"
done
```

Verify batch 1:
```bash
for repo in clean-scraper-extension fo4_addonideas github-for-developers-7 imatch-deviantart-api imatch-imgops tabbles-tools tabbles4-plugin-wlpg; do
  gh repo view novakda/$repo --json isPrivate --jq '.isPrivate'
done
```
All should return `true`.

Step 3 — Make 24 non-eLearning forks private (batch 2):
```bash
for repo in agnai booru-browser-vue CAI-Tools character-tools danbooru git-sticker hydrus hydrus.js JSON-js logseq my-box nvs omnivore RisuAI SillyTavern silverbullet-manager-space-template spa_react_javascript_hello-world Spoon-Knife tabbles-testPlugin-ApiV4 tiny-1 turbo-vue-storybook tuskydesign typescript-EssT-2428199 tzk; do
  gh repo edit novakda/$repo --visibility private
  echo "Made private: $repo"
done
```

Verify batch 2:
```bash
for repo in agnai booru-browser-vue CAI-Tools character-tools danbooru git-sticker hydrus hydrus.js JSON-js logseq my-box nvs omnivore RisuAI SillyTavern silverbullet-manager-space-template spa_react_javascript_hello-world Spoon-Knife tabbles-testPlugin-ApiV4 tiny-1 turbo-vue-storybook tuskydesign typescript-EssT-2428199 tzk; do
  gh repo view novakda/$repo --json isPrivate --jq '.isPrivate'
done
```
All should return `true`.

Step 4 — Verify only 8 repos remain public:
```bash
gh repo list novakda --visibility public --json name --jq '.[].name' | sort
```
Expected: aria-practices, dewordify, get-shit-done, lms-content-template-scorm2004e3, novakda, pattern158.solutions, SCOBot, wai-tutorials (8 repos).

Note: Some repos in the list may already be private or may not exist. If `gh repo edit` returns an error for a repo, log it and continue — do not abort the batch. The goal is that only the 8 designated repos remain public.
  </action>
  <verify>
1. `gh repo list novakda --visibility public --json name --jq '.[].name' | sort` returns exactly 8 repos
2. before-state.json file exists in phase directory
3. All 7 original repos return isPrivate=true
4. All 24 fork repos return isPrivate=true (or were already private/don't exist)
  </verify>
  <done>31 repos are private, only 8 designated repos remain public, baseline snapshot saved for rollback reference</done>
</task>

<task type="auto">
  <name>Task 2: Update pattern158.solutions and novakda repository metadata</name>
  <files></files>
  <action>
Step 1 — Update pattern158.solutions repo metadata:

Description: Use a minimal description per user decision ("let the homepage URL speak for itself"). Include Pattern 158 brand name per user decision ("Brand name only in the pattern158.solutions repo description"). Hybrid tone: lead with outcome, include key tech terms.

```bash
gh repo edit novakda/pattern158.solutions \
  --description "Pattern 158 Solutions — eLearning systems architecture and accessibility portfolio" \
  --homepage "https://pattern158.solutions"
```

Step 2 — Add topics to pattern158.solutions:

Per research recommendations: `portfolio`, `accessibility`, `wcag`, `static-site`. These are domain topics that match the target audience (hiring managers, eLearning professionals). Skip tech-stack topics (html, javascript, css) — they don't differentiate this repo for the target audience.

```bash
gh repo edit novakda/pattern158.solutions \
  --add-topic "portfolio" \
  --add-topic "accessibility" \
  --add-topic "wcag" \
  --add-topic "static-site"
```

Verify pattern158.solutions:
```bash
gh repo view novakda/pattern158.solutions --json description,homepageUrl
gh api /repos/novakda/pattern158.solutions/topics -H "Accept: application/vnd.github.mercy-preview+json" | jq .names
```

Step 3 — Update novakda profile README repo description:

The novakda repo serves as the profile README. Give it a descriptive one-liner. This satisfies META-04 ("All visible original repos have descriptive one-liner descriptions") for the novakda repo.

```bash
gh repo edit novakda/novakda \
  --description "GitHub profile README — Dan Novak, eLearning systems architect"
```

Verify novakda:
```bash
gh repo view novakda/novakda --json description
```

Both repos should now have professional descriptions. pattern158.solutions should also have homepage URL and 4 topics.
  </action>
  <verify>
1. `gh repo view novakda/pattern158.solutions --json description,homepageUrl` shows correct description and homepage URL
2. `gh api /repos/novakda/pattern158.solutions/topics -H "Accept: application/vnd.github.mercy-preview+json"` returns 4 topics
3. `gh repo view novakda/novakda --json description` shows profile README description
  </verify>
  <done>pattern158.solutions has professional description with Pattern 158 brand name, homepage URL set to https://pattern158.solutions, and 4 domain-specific topics applied. novakda repo has descriptive one-liner. META-01, META-02, META-03 fully satisfied. META-04 partially satisfied (2 of 2 original repos complete).</done>
</task>

</tasks>

<verification>
1. Exactly 8 repos are public: `gh repo list novakda --visibility public --json name --jq '.[].name' | sort`
2. pattern158.solutions shows description, homepage, and 4 topics on GitHub
3. novakda shows descriptive one-liner on GitHub
4. before-state.json exists as rollback reference
5. No errors logged during private visibility transitions
</verification>

<success_criteria>
- 31 repos made private (only 8 remain public)
- pattern158.solutions has description containing "Pattern 158" and "eLearning"
- pattern158.solutions homepage URL is https://pattern158.solutions
- pattern158.solutions has topics: portfolio, accessibility, wcag, static-site
- novakda repo has descriptive one-liner
- Baseline snapshot saved to before-state.json
</success_criteria>

<output>
After completion, create `.planning/phases/20-repository-metadata-discoverability/20-01-SUMMARY.md`
</output>
